# 일꾼 키우기 — 작업 인수인계

마지막 갱신: 2026-08-11  
목적: 새 대화 또는 다른 컴퓨터에서 이 문서만 읽고 개발을 이어갈 수 있게 현재 MVP 상태를 기록한다.

## 빠른 시작

```bash
git clone https://github.com/bigtory-web/idleWeapon.git
cd idleWeapon
npm install
npm run dev
```

- GitHub 원격: `https://github.com/bigtory-web/idleWeapon.git`
- 기본 브랜치: `main`
- 배포 주소: `https://worker-backpack-battle.pow16.chatgpt.site/?seed=prototype-001`
- 기본 시드: `prototype-001`
- 현재 기준: GitHub `main`의 최신 배포본
- 배포 프로젝트 설정: `.openai/hosting.json`에 보관한다. 값은 변경하지 않는다.

## 게임 한 줄 설명

가방 안에서 캐릭터와 다칸 장비를 인접 배치해 이름 있는 장비 조합을 완성하는 세로형 자동전투 게임이다.

## 현재 게임 흐름

`준비 → 전투 → 웨이브 상점 → 다음 전투 → 보스 → 결과`

- 전투 중에는 가방을 편집할 수 없다.
- 웨이브를 끝내면 골드를 받고, 상점의 상품별 `N골드` 버튼으로 가방 첫 빈 위치에 구매한다.
- 구매품은 항상 첫 빈 공간에 별도 생성된다. 같은 종류·티어 아이템을 직접 겹칠 때만 한 단계 합성한다.
- 보스 웨이브 뒤에는 상점을 열지 않는다.

## 가방 규칙

### 아이템 크기와 회전

| 종류 | 모양 | 회전 |
|---|---|---|
| 척후병 | 1칸 | 불가 |
| 방패병 | 1칸 | 불가 |
| 명사수 | 1칸 | 불가 |
| 검 | 가로 2칸 | 시작 방향 고정 |
| 마법봉 | 가로 2칸 | 시작 방향 고정 |
| 활 | 가로 3칸 | 시작 방향 고정 |
| 망치 | 3칸 L자 | 시작 방향 고정 |
| 방패 | 세로 2칸 | 시작 방향 고정 |
| 마법책 | 가로 2칸 | 시작 방향 고정 |

- 가방은 7×5이며 기본 셀은 42×42px, 간격은 4px이다.
- 사용자 회전 버튼과 `R` 키 조작은 없다. 시작 세로 활처럼 데이터에 저장된 방향은 그대로 지원한다.
- 새 상점 무기는 기본 방향으로만 자동 배치한다.
- 다칸 아이템의 기준점은 회전 후 도형의 좌상단이다.
- 드래그 중 유효 위치는 청록, 충돌·범위 초과는 빨강으로 보인다.
- PC에서는 호버·키보드 포커스 시 전투 화면 하단의 고정 도움말 바에 상세 정보를 표시하고, 5px 이상 이동은 드래그로 구분한다.

### 인접 장착과 조합

- 캐릭터와 무기의 점유 칀이 상하좌우로 맞닿으면 장착된다. 대각선은 인정하지 않는다.
- 소켓 데이터와 `○` 표시는 없다.
- 맞닿은 무기는 캐릭터 티어와 관계없이 모두 장착된다. 장착 여부를 나타내는 별도 청록 강조는 표시하지 않는다.
- 1칸 캐릭터는 사방에 연결된 최대 4개 무기를 동시에 사용할 수 있다.
- 전투 시작 시 물리적으로 연결된 모든 무기와 활성 장비 조합이 스포너에 전달된다.

### 가방 UI 방향

- 무기 블록에는 이름이나 `T1` 같은 글자를 넣지 않으며, 캐릭터는 임시 식별용 이름만 이모지 아래에 표시한다.
- 다칸 아이템은 점유 칸과 4px 접합 간격까지 하나의 불투명 BG·외곽선으로 묶고, 아이콘은 중앙에 하나만 둔다. 망치 L자의 빈 네 번째 칸은 실제로 비어 있다.
- 티어는 외곽선 색뿐 아니라 두께·광택·배경으로 구분한다.
- 장착된 무기는 캐릭터 중앙 아이콘 주변의 작은 아이콘으로 표시한다.
- 상세 능력치·패널티·장착 상태는 가방과 상점이 공유하는 아이콘 포함 고정 호버 도움말에서 보여준다.

## 캐릭터와 장비

### 캐릭터

| 캐릭터 | 기본 HP | 이동 | 생성 쿨 | 분대 상한 T1/T2/T3 | 특징 |
|---|---:|---:|---:|---:|---|
| 방패병 | 180 | 34 | 6.0초 | 2 / 3 / 5 | 근접 피해에 강함 |
| 척후병 | 80 | 52 | 3.8초 | 4 / 6 / 9 | 빠른 증원, 낮은 내구 |
| 명사수 | 90 | 38 | 5.2초 | 2 / 3 / 5 | 원거리 피해·사거리 특화 |

- 웨이브 시작 즉시 생성하지 않는다. 각 스포너는 쿨타임을 전부 채운 뒤 생성한다.
- 분대가 상한에 도달하면 쿨타임을 멈춘다.
- 해당 분대원이 죽으면 0%부터 다시 충전하여 한 명씩 충원한다.
- 전체 유닛 상한 때문에 생성할 수 없으면 100%에서 대기한다.

### 무기와 패널티

| 무기 | 역할 | 패널티 / 특수 상성 |
|---|---|---|
| 검 | 근거리 2명 베기 | 이동속도 -6% |
| 활 | 장거리 단일 투사체 | 체력 -10% |
| 망치 | 근거리 광역 | 이동속도 -14%, 갑옷병 방어 무시 |
| 마법봉 | 장거리 연쇄 공격 | 체력 -8% |
| 방패 | 근거리 막기·밀치기 | 이동속도 -10% |
| 마법책 | 장거리 마법탄·조합 재료 | 체력 -6% |

장착 패널티는 실제 생성 아군의 최대 체력과 이동속도에 곱연산으로 적용된다.

### 이름 있는 장비 조합

- 연결된 장비로 완성되는 모든 조합을 동시에 활성화한다.
- 교차 조합은 종류당 한 번, 같은 장비 2개는 전용 조합을 한 번 적용한다.
- 초기 12종: 쌍검, 연발궁, 지진술사, 과충전, 철벽, 대마도서, 선봉대, 비전 방벽, 마검사, 마력 화살, 철갑 파쇄자, 대마법사.
- 조합 효과와 대가는 `lib/game/combos.ts`가 단일 기준이며 가방·상점 도움말에 표시한다.

## 전투와 적 역할

- 논리 전투는 X축 전선 + 깊이(Y/Z 느낌) 레인을 사용한다. 보드 열은 `x=58..154`, 행은 `y=230..310`으로 생성 위치에 반영된다.
- 화면은 Canvas 2D 기반 중간 탑다운 2.5D 시점이며, 실제 판정과 이동은 깊이 축까지 사용한다.
- 렌더 크기는 게임 판정과 분리되어 있으며, 아군·적 외곽선은 각각 청록·빨강이다.
- 캐릭터 배치마다 비공격형 소환대가 표시되고, 생성 시 장착 무기 아이콘이 캐릭터 중심으로 흡수된다.
- 전투 바닥 왼쪽에는 가방과 대응하는 7×5 배치 타일이 표시되고, 소환대는 기존보다 크게 그린다.
- 기지 HP 숫자와 상단 게이지는 제거하고 기지 하단의 소형 게이지만 Canvas에 그린다.
- 전투 배속은 0.5×/1×/2×이며 선택값을 기기에 저장한다. 2×도 최대 0.05초 단위로 나눠 시뮬레이션한다.
- 각 웨이브의 일반 적은 기존 그룹 간격 없이 시작부터 0.15초마다 순차 생성한다. 보스는 모든 일반 적이 전멸한 다음 생성한다.
- 아군은 원래 배치 행을 부드럽게 유지하고 같은 행의 적을 우선한다. 방패병은 근접 도발, 척후병은 투척병 우선, 명사수는 원거리 유지 역할을 가진다.
- 검은 전열, 활은 낮은 체력, 망치는 밀집 구역, 마법봉은 연쇄 효율이 높은 표적을 선택한다.
- 양 진영 기본 이동속도는 이전 대비 약 20% 낮췄고, 아군 최소 간격은 14, 적 최소 간격은 16으로 넓혀 겹침을 완화했다.
- 2웨이브부터 일반 적 수는 기존 구성 비율을 유지하며 총량을 약 70%로 축소한다. 6웨이브 보스 1마리는 감축 대상에서 제외한다.
- 분대 상한은 방패병 T1/T2/T3 `1/1/1`, 척후병 `2/4/6`, 명사수 `2/3/5`다.

| 적 | 현재 행동 역할 | 플레이어가 읽어야 할 대응 |
|---|---|---|
| 졸개 | 기본 전열 | 기본 화력 시험 |
| 질주병 | 접근 중 1.35배 속도로 압박 | 원거리·빠른 처리 필요 |
| 갑옷병 | 높은 HP와 방어 | 망치가 방어를 무시해 유리 |
| 투척병 | 가장 낮은 최대 HP 아군을 우선 조준 | 후방 위협, 먼저 처리할 대상 |
| 보스 | 전열이 비면 기지에 1.5배 피해 | 전열 유지가 핵심 |

현재 적 공통 강화는 HP ×1.2, 공격력 ×1.1이다. 제한 시간은 유지하며 2웨이브 이후 적 수와 양 진영 이동속도만 추가 완화했다.

상단 HUD에는 웨이브와 보유 골드를 표시하고, 제한/남은 시간은 전투 화면 중앙 상단에 표시한다. 고정 호버 도움말은 높이 104px이며 캐릭터 생성 주기는 우측 상단 배지, 무기는 공격력·초당 공격 횟수·사거리·대상 수·장착 효과를 보여준다. 무기의 공유 캐릭터·인접 장착 문구와 캐릭터의 장착 무기 목록은 표시하지 않는다.

## 주요 파일 지도

| 경로 | 역할 |
|---|---|
| `app/GameClient.tsx` | React 상태, 가방 드래그/상점/설정/HUD |
| `app/globals.css` | 보드 UI, 다칸 블록, 연결·호버 툴팁 스타일 |
| `lib/game/types.ts` | 공용 게임 타입 |
| `lib/game/data.ts` | 캐릭터·무기·적·웨이브·시작 가방 데이터 |
| `lib/game/inventory.ts` | 점유 칸, 인접 장착, 직접 합성, 배치 순수 함수 |
| `lib/game/combos.ts` | 12개 이름 있는 장비 조합과 활성 판정 |
| `lib/game/engine.ts` | 60Hz 전투 시뮬레이션, 스포너, 적 AI |
| `lib/game/render.ts` | Canvas 2.5D 전장 렌더링 |
| `lib/game/battle-layout.ts` | 7×5 보드와 전투 좌표 공통 매핑 |
| `lib/game/speed.ts` | 배속 정규화와 안전한 프레임 분할 |
| `tests/game-logic.test.ts` | 인벤토리/스포너/결정론 테스트 |
| `tests/rendered-html.test.mjs` | UI 구조·렌더 구성 회귀 테스트 |

## 검증 명령

```bash
npm run test:game
npm run build
node --test tests/rendered-html.test.mjs
```

마지막 검증 기준:

- 게임 로직: 26개 통과
- 렌더·서버 구조: 10개 통과
- `vinext build`: 통과

## 배포 절차

이 프로젝트는 Sites 설정이 있으므로 변경 후 빌드가 통과하면 기존 프로젝트에 새 버전을 저장·배포한다.

1. `npm run build`
2. GitHub `main`에 커밋·푸시
3. Sites 배포 도구로 `dist/`를 패키징하고 기존 project ID에 버전을 저장
4. 기존 주소로 배포 상태를 확인

사이트 URL은 계속 `https://worker-backpack-battle.pow16.chatgpt.site/`를 사용한다.

## 다음 대화에서 우선 확인할 것

1. 모바일 실제 기기에서 다칸 드래그와 직접 겹치기 합성을 확인
2. 첫 5명 플레이테스트: 상점에서 다음 조합 재료를 고민하고 배치를 바꾸는지 확인
3. 질주병·갑옷병·투척병·보스가 서로 다른 위협으로 읽히는지 전투 영상/플레이에서 점검
4. 필요하면 수치 조정은 **전투 데이터만** 바꾸고, 가방 규칙과 UI 언어 없는 방향은 유지

## 이번 단계에서 제외한 항목

- 필드 무기 드롭·줍기
- 전투 중 가방 편집
- 새 캐릭터·무기
- 상태 이상, 판매, 리롤, 영구 성장
- 패배 사유 문구·상점 다음 웨이브 예고·추가 적 기믹

---

## 2026-08-12 최신 인수인계

현재 배포 주소: `https://worker-backpack-battle.pow16.chatgpt.site/`

최신 구현 커밋: `b12fce8 Add T5 equipment progression and staged waves`

### 이번에 완료한 기능

- 아이템 티어를 T5까지 확장했다. T4는 보라색, T5는 붉은색 외곽선을 사용한다.
- 동일 종류·동일 티어 아이템을 직접 겹칠 때만 합성된다. T5끼리는 더 이상 합쳐지지 않는다.
- 같은 종류·티어의 합성 가능한 짝이 있으면 두 아이템 위에 작은 `↑` 표시가 나타난다.
- 기존 12종 장비 조합은 캐릭터와 필요한 무기가 모두 T5일 때만 발동한다. T4 이하 장비는 개별 장비 효과만 적용한다.
- 캐릭터 T4/T5 HP·공격 배율은 `3.2/4.0`, 무기 피해 배율은 `3.6/4.5`, 생성시간 배율은 `0.75/0.7`이다.
- 분대 상한은 T3 이후 증가하지 않는다. 방패병 `1/1/1/1/1`, 척후병 `2/4/6/6/6`, 명사수 `2/3/5/5/5`다.
- 무기에 장비 코스트가 추가됐다. 검·마법봉·방패·마법책은 2, 활·망치는 3이다.
- 캐릭터 최종 생성시간은 `기본 생성시간 × 티어 배율 × (1 + 총 코스트 × 0.1)`이다. 호버에 총 코스트와 최종 생성시간을 표시한다.
- 적은 첫 그룹부터 0.15초 간격으로 등장한다. 현재 그룹이 전멸하면 다음 그룹이 즉시 열리고, 적이 남아 있으면 데이터의 6~8초 간격 후 강제로 다음 그룹이 열린다.
- 보스는 모든 일반 그룹이 열리고 대기·생존 일반 적이 모두 사라진 뒤 생성된다.
- 상점 제목 옆에 무료·무제한 `↻ 리롤` 버튼이 있다. 결과는 시드·웨이브·리롤 횟수로 결정된다.
- 상단 보유 골드 옆에 테스트용 `+100` 버튼이 있다. 이 골드는 골드 획득 통계에 포함되지 않는다.

### 검증 및 배포 상태

- `tests/game-logic.test.ts`: 28개 통과
- `tests/rendered-html.test.mjs`: 11개 통과
- `vinext build`: 통과
- 실제 배포 화면에서 `+100`, 2배속 전투, 웨이브 1 클리어, 상점 진입, 무료 리롤 상품 교체를 확인했다.
- GitHub `main`과 Sites 소스 저장소에 동일 커밋을 푸시했고 Sites 버전 24를 배포했다.

### 다음 대화에서 먼저 확인할 것

1. T5까지 수동 합성하는 플레이 시간이 재미 검증에 적절한지 확인한다. 현재 상점은 T1만 판매하므로 테스트에는 리롤과 `+100` 버튼을 사용한다.
2. 합성 가능 `↑`가 다칸 아이템 및 모바일 화면에서도 가독성이 좋은지 확인한다.
3. 장비 코스트 2/3과 생성시간 증가율 10%가 다중 장착 선택을 충분히 고민하게 만드는지 플레이 테스트한다.
4. 적 그룹이 전멸 시 즉시 이어지고, 장기 교전에서는 6~8초 후 겹쳐 들어오는 압박이 적절한지 확인한다.
